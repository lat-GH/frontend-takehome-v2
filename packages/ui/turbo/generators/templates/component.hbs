import { FunctionComponent, ReactNode } from 'react';

interface {{ pascalCase name }}Props {
  children: ReactNode;
}

const {{ pascalCase name }}: FunctionComponent<{{ pascalCase name }}Props> = ({ children }) => (
  <div>
    {children}
  </div>
);

export default {{ pascalCase name }};
